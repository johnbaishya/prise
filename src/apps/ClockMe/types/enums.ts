export enum clockStatus{
    active="active",
    inactive="inactive",
    completed="completed",
    cancelled="cancelled",
}

export enum ClockActionType {
    clock_out="clock_out",
    break_start="break_start",
    break_end="break_end",
}